# Portable dev42 — 2026-09-22

- По запросу пользователя собрана `dist/20260922-183758/Gesture-Control-0.1.0.dev42-win64.zip`, 123.3 MiB. SHA256: `48187b5fe57467db108e9852de56702e7d1e2c141a4f23b088472df21598768a`.
- Frozen self-test прошёл из отдельного каталога с Python, исключённым из PATH: модель, профили и руна, inference OpenCV/MediaPipe, spawn watchdog с fake input, GUI и нативный трей. CRC ZIP проверен; камера и реальный ввод в самотесте не запускались. Предыдущие сборки сохранены.

# Красный левый клик для покадровой обработки — 2026-09-22, исходники

- 354 теста прошли. Наведение красного указателя предшествует левому down минимум на 40 мс; подтверждённый короткий щипок сохраняется, down/up разнесены минимум на 40 мс. Возврат к синей позиции после up отложен на 120 мс. Всё выполняется на свежих кадрах, без sleep и без удержания кнопки после потери руки.
- Проверены порядок и времена MOVE/DOWN/UP/restore, сохранение позиции для позднего чтения, отмена ожидающего down при потере руки и аварийной паузе. Существующие drag/Zoom/правые клики проходят.
- Это исправление потенциальной несовместимости с покадровой обработкой указателя. Название пользовательской игры пока неизвестно, реальная игра не проверялась. Portable не собиралась.

# Щипок красной руны, антидрожь и скорость — 2026-09-22, исходники

- 351 тест прошёл. Разделены эффекты нажатий двух рун. Проверены сохранение подтверждённой второй руки при непрерывной геометрии и падении side-confidence, успешные down/up, запрет первоначального захвата новой руки с недостаточной уверенностью.
- Красная руна использует CursorMotion и отображаемую позицию как координаты клика. Опциональный suppress_jitter (по умолчанию false) удерживает цель в радиусе jitter_radius_px (3 пикселя); проверены неподвижность при шуме, медленное накопленное движение, достижение края, отключение фильтра и привязка красного нажатия к отрисовываемой позиции.
- Ползунок cursor_max_speed расширен с 4 до 10 в пределах существующей валидации. Проверена разница ограничения скорости. Нативный снимок главного окна проверен: кнопка «Антидрожь» помещается в блоке «Плавность».
- Все вводы в тестах синтетические. Пользовательский случай на реальной камере ещё требует проверки. Portable не собиралась.

# Перетаскивание второй рукой и обратное переключение — 2026-09-22, исходники

- 345 тестов прошли. Левый щипок красной руки удерживает кнопку с координатами красной руны; относительное движение центра ладони фильтруется и использует порог drag_start_distance. Синяя руна остаётся на месте до завершения захвата.
- Проверены обе анатомические стороны в роли второй руки, отсутствие влияния движений основной руки, ровно одно нажатие/отпускание, потеря руки с обязательным раскрытием перед повтором, аварийная пауза, приоритет захвата над Zoom и отказ позиционирования до нажатия.
- Отдельный распознаватель взмаха для второй руки; анатомическая левая рука использует PREVIOUS_APP (Alt+Shift+Tab в Desktop) независимо от цвета руны. Проверены точные пары down/up модификаторов и запрет переключения во время захвата основной рукой. Правые направления сохранены.
- Тесты используют синтетические координаты и fake input. Перетаскивание реального окна и удобство на камере требуют пользовательской проверки. Portable не собиралась.

# Правый клик красной руны — 2026-09-22, исходники

- 338 тестов прошли. После правого клика вторичной руки возврат системного указателя к синей позиции отложен на 250 мс по времени кадров, без sleep. В этот период движение основной руки не перезаписывает позицию; синяя руна отображается в сохранённой точке. Пауза отменяет отложенный возврат.
- Регрессия имитирует чтение позиции указателя приложением после обработки button-up, при движущейся основной руке. Проверены последующий возврат и отмена при паузе. Реальное контекстное меню пользователя не тестировалось; 250 мс — ограниченное окно для обработки, не подтверждение от целевого приложения.
- Portable не собиралась по просьбе пользователя.

# Центр рун и скрытие курсора — dev41

- Portable `dist/20260922-172601/Gesture-Control-0.1.0.dev41-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `66a7eb3df884ed269ebc24ad4523a7cd4fc974984f482f3fbc3b8de2670aad0b`.
- 336 тестов прошли. Проверены условия скрытия (ACTIVE, TRACKING, отрисованная руна, Visual FX включены), сохранение и восстановление курсоров, частичный отказ и повтор восстановления, остановка и истечение heartbeat watchdog.
- Windows API скрытия проверен через mock: настоящий системный курсор во время тестов не скрывался. Проверка на пользовательской машине остаётся ручной.
- Native smoke отрисовки точки на рунах FULL/MINIMAL, включая золотую, прошёл. Экран настроек проверен по снимку нативного окна.
- Копии исходных курсоров сохраняются перед заменой; правила владения дескрипторами сверены с [Microsoft SetSystemCursor](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setsystemcursor).

# Владение прокруткой обеих рук — dev40

- Portable `dist/20260920-081526/Gesture-Control-0.1.0.dev40-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `7c5beb5e46b91d9c89947760726ae4a55cbd296f5f6a9f160a93c5743fa03d9f`.

- 331 тест прошёл. Синяя и красная рука используют явный режим владения после среднего клика. Жесты неуправляющей руки не выполняют клики/Zoom/команды окон и не отменяют прокрутку. Остановка требует подтверждённого жеста владельца, паузы или потери отслеживания.
- Проверены движения при удержании двух пальцев, повторный средний жест как завершение без нового клика, игнорирование синего щипка при красной прокрутке, игнорирование красных кликов и тройной щепотки при синей, длинное снижение side-confidence при непрерывной геометрии, краткий пропуск и длительная потеря красной руки. Fallback не выбирает новую руку и ограничен прежней стороной, размером, разделением рук и непрерывностью положения.
- Автопрокрутка по-прежнему выполняется целевым приложением. Проверки синтетические, живой браузер и камера не использовались для приёмки.

# Автопрокрутка красной рукой — dev39

- Portable `dist/20260920-080530/Gesture-Control-0.1.0.dev39-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `a2ea496f3069047bd7970afef8345f8a148f6522f37a87ef09fe7df01efde542`.

- 328 тестов прошли. После среднего клика красной руки ввод следует за её координатами; движение основной руки не перемещает системный указатель. Проверены завершение щипком основной руки без лишнего клика, потеря вторичной руки, Esc при паузе и возврат указателя в исходную синюю точку.
- Native smoke Windows: синяя руна отображается по сохранённой позиции во время красного управления и затем возвращается к сопровождению системного указателя. Реальные клики/прокрутка браузера в тестах не выполнялись.
- Автопрокрутка по-прежнему принадлежит целевому приложению. Режим красного управления включается после любого среднего клика второй рукой; завершение посылает Esc. Для среднего клика по ссылке пользователь также завершает режим жестом. Это описано в обновлённой инструкции.

# Координаты красного клика в Windows — dev38

- Portable `dist/20260920-075754/Gesture-Control-0.1.0.dev38-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `d6dc158c01b01765481e40d108ce3961e6d90ca35f51aefe15bb03c55e723d84`.

- 324 теста прошли. Native backend формирует для каждого down/up второй руки пакет MOVE | ABSOLUTE | BUTTON с координатами красной руны. Тесты проверяют dx/dy и флаги всех трёх кнопок через перехваченный SendInput; просроченные и выходящие за экран команды не инжектируются.
- Интеграционная проверка дополнительно требует position в каждом кнопочном событии второй руки. Вызовы реальных кнопок Windows не выполнялись; пользовательское наблюдение требуется для подтверждения исправления в целевом приложении.
- Инструкция переписана как руководство актуального поведения: добавлена краткая памятка, автоматические роли рук, координаты кликов, золотой жест обеих рук, запуск из проекта и диагностика. Старые заметки версий и описание дополнительного ожидания Zoom удалены.

# Золотая руна обеих рук — dev37

- Portable `dist/20260920-075046/Gesture-Control-0.1.0.dev37-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `62a8b46559ac51f8213a01128f4198a104cc6c0cd989c0744dba180903d088f3`.

- 322 теста прошли. Добавлены команды основной руки при отключённой второй, золотая готовность после подтверждения, одновременная щепотка обеих рук без дублирования Win+Tab и без Zoom/клика, выключение рабочего жеста на основной руке. Существующие проверки drag и Zoom проходят.
- Native smoke Windows: синяя и красная руны переходят в золотую и обратно, FULL/MINIMAL; ошибок оверлея нет. До подтверждения неподвижной позы основной курсор продолжает наведение. Золотая готовность фиксирует его на время рабочего жеста.
- Тройная щепотка имеет приоритет над новым кликом/Zoom, но не перехватывает уже начатое нажатие или активный Zoom. При одновременной тройной щепотке приоритет у основной руки. На живой камере остаётся проверить ложные распознавания.

# Одиночные щипки при двух руках — dev36

- Portable `dist/20260920-074511/Gesture-Control-0.1.0.dev36-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `d641300f172679e154dfa9e5e55b77991cd3b251ee5caf0a2f5e1e0c076ace88`.

- 319 тестов прошли. Добавлены короткие левый/правый щипки каждой руки при видимой второй, а также левый щипок основной руки с поднятым средним пальцем. Удалено ожидание WAIT, которое поглощало короткий одиночный щипок основной руки. Два контакта большим/указательным по-прежнему входят в Zoom без предварительного клика.
- Сохранены тесты координат красной руны, очистки ввода, drag, двойного клика, рабочих столов и автоматической смены основной руки. Требуется ручная проверка удобства почти одновременного входа в Zoom; дополнительное окно 200 мс больше не используется.

# Щепотка второй руки — dev35

- Portable `dist/20260920-073731/Gesture-Control-0.1.0.dev35-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `29cd373526068a5c919b06a3a0d390d230e46b05c133f1163f693025534acc3a`.

- 317 тестов прошли: щепотка большого/указательного/среднего пальцев после 300 мс удержания, движения вверх/вниз, запрет повторения до размыкания обоих контактов, отсутствие клика при частичном раскрытии, отсутствие команд от кулака второй руки. Проверки обычных кликов, Zoom и автоматической смены основной руки сохранены.
- Native smoke оверлея Windows: красный → золотистый WORKSPACE_READY → красный, режимы FULL и MINIMAL, ошибок нет. Реальные клики/сочетания Windows не отправлялись; распознавание на живой камере требует проверки пользователем.
- Профильные идентификаторы FOUR_SWIPE_UP / FOUR_SWIPE_DOWN сохранены для совместимости; запускаются теперь тройной щепоткой.

# Автоматический выбор основной руки — dev34

- Portable `dist/20260920-072624/Gesture-Control-0.1.0.dev34-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `96832970ff75355844621521fceb463ebf36a5092f2405b9ed264eb1879346cd`.

- 315 тестов прошли. Интеграционный сценарий: синяя правая + красная левая → только левая открытая → синяя левая + вернувшаяся красная правая → только правая открытая. В обе стороны восстановлено ACTIVE, оставшаяся рука становится основной, одиночная красная руна скрывается.
- В пользовательском и примерном конфиге возвращён FIRST. Алгоритм безопасности восстановления сохранён: новая основная рука подтверждается открытой ладонью, старые нажатия не переносятся. Основная кликает в синей точке, вторичная — в красной; сторона руки не фиксирует цвет.

# Независимые точки клика — dev33

- Portable `dist/20260920-071910/Gesture-Control-0.1.0.dev33-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `3d609cb61252434b41c85a6d3a499e7b8e0ab7d1e4218176a4b7928dbe1f45be`.

- 314 тестов прошли. Координатная проверка всех трёх кнопок второй руки теперь требует нажатия/отпускания в красной точке и возврата указателя в синюю. Проверено, что ошибка позиционирования отменяет отправку кнопок; проверки очистки ввода при ошибке отпускания также проходят.
- В settings.user.json и settings.example.json основная рука выбрана RIGHT. Старые внешние конфиги сохраняют собственный выбор руки.
- Проверки используют fake input; реальную работу приложений Windows при кратком переносе указателя нужно проверить вручную. В частности, средний клик с возвратом указателя может влиять на автопрокрутку браузера.

# Щипки второй рукой — dev32

- Portable `dist/20260919-233949/Gesture-Control-0.1.0.dev32-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `b976b4a5d74bc89d0208acc07de411efd9c67258561e3c1cf28045734e6142ab`.

- 313 тестов прошли. Добавлены короткие левый/правый щипки при двух поднятых пальцах, подавление одиночного кадра контакта, отмена при потере руки, средний клик только с разомкнутым большим пальцем и отсутствие повторного события при удержании среднего жеста.
- Существующие проверки координат трёх кнопок, Zoom, кулака второй руки и очистки ввода проходят. Реальную руку пользователя этот прогон не проверяет; уверенность выбора второй руки и пороги контакта не снижались.

# Кулак второй руки и координаты кликов — dev31

- Portable `dist/20260919-232613/Gesture-Control-0.1.0.dev31-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `66b39f2ac4190dd5acb98c3be7524af2779cd23efe8cd6727847d0b5f3d1c14c`.

- 309 тестов прошли. Workspace использует closed_fist и удержание 400 мс. Четыре пальца второй руки больше не вызывают команды. Проверены медленный/быстрый ход в обе стороны, обязательное раскрытие ладони перед повтором и отсутствие клика/Esc от кулака второй руки.
- Координатная проверка трёх кнопок: при разнесённых руках пары down/up второй руки приходят в позицию основного системного курсора, отличную от позиции красной руны. Проверены fake backend и распознавание синтетических рук; живую камеру и реальные нажатия этот прогон не проверяет.
- Имена профильных привязок FOUR_SWIPE_UP / FOUR_SWIPE_DOWN оставлены для совместимости сохранённых профилей; жест, вызывающий их, теперь кулак второй руки.

# Команды второй рукой — dev30

- Portable `dist/20260919-231535/Gesture-Control-0.1.0.dev30-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `024640f84c8ab084fe13e63dd5bb9ba4660e5ba90a3ad93c4ae9addc887377b8`.

- 305 тестов прошли. Проверены запрет рабочих столов на основной руке, подтверждение позы на второй перед быстрым/медленным движением, левый/правый/средний клик второй рукой, отмена при потере, выключение второй руки, приоритет средней кнопки основной руки и очистка ввода при ошибке отпускания. Предыдущие тесты Zoom без предварительного клика и защиты активного drag проходят.
- Вторичная рука использует строгое сопоставление двух разных рук. Красная руна может отображаться при низкой уверенности стороны, но команды при этом не разрешаются. Клики отправляются в точке системного курсора короткой парой down/up; вторичного drag нет.
- Предыдущий код и пользовательские настройки сохранены в `build/backups/dev29-before-secondary-actions`. Живую камеру и реальные команды Windows в тестах не использовали.

# Более лёгкие жесты четырёх пальцев — dev29

- Portable `dist/20260919-225644/Gesture-Control-0.1.0.dev29-win64.zip`: frozen self-test и CRC архива прошли, 123.3 MiB, SHA256 `85086e86a03fccfc26fa03be5fa8b4e3a4f3de897c181822ad947f38c8bc9369`.

- 299 тестов прошли. Новые сценарии: медленные движения длительностью 1,4 с в обе стороны, слегка согнутые четыре пальца, менее плотно подогнутый большой палец, небольшой боковой уход, быстрый жест, дрожание на месте, обычная открытая ладонь и немедленный обратный ход. Интеграционная проверка подтверждает ровно одну последовательность Win+Tab / Win+D с освобождением клавиш.
- WorkspaceGesture использует отдельное окно 1600 мс и порог остановки .025; настройки горизонтального смахивания не менялись. Старые workspace-пороги заменены в settings.user.json с резервной копией в build/backups/dev28-before-four-fingers.
- Тесты используют синтетические руки и fake input; комфорт на живой камере остаётся предметом пользовательской проверки.

# Клики и освобождение наведения — dev28

- Portable `dist/20260919-225125/Gesture-Control-0.1.0.dev28-win64.zip`: frozen self-test и CRC ZIP прошли, 123.3 MiB, SHA256 `5c9abb5f27c34722a223034256117d40f26158ea9492bc378e623e2a5d92cb7c`.

- 293 теста прошли. Добавлены проверки движения ладони при неоднозначном новом щипке без отправки кнопок, медленного непрерывного сближения без преждевременного снятия фиксации, отсутствия паузы после drag и раннего выхода из ожидания второго клика при последовательном движении. Проверка дрожания сохраняет ожидание двойного клика.
- Предыдущий код interaction.py, pinch_aim.py и double_click.py сохранён в `build/backups/dev27-before-click-refinement`; прежняя portable-сборка сохранена.
- Точность на живой камере и субъективная лёгкость клика не подтверждаются синтетическими тестами. Удержание правого щипка по-прежнему фиксирует курсор до размыкания; это поведение не менялось.

# Точное наведение и исправления двух рук — dev27

- 288 тестов прошли: добавлены подавление мелкого дрожания, сохранение медленного перемещения и достижения края, уменьшение отставания при быстром движении, приоритет среднего клика при контакте большого пальца и отображение второй руки с низкой уверенностью стороны без допуска Zoom.
- Проверен реальный Tk-интерфейс со снимком в размере 1100×720. Новые переключатели и статус помещаются над камерой.
- Portable: `dist/20260919-224044/Gesture-Control-0.1.0.dev27-win64.zip`, 123.3 MiB, SHA256 `2e3c3f1935f3bc5bcebad6670ac4be8c85682d1fca52fd74a95e14af7d92b16c`. Frozen self-test и проверка ZIP прошли. Предыдущие сборки сохранены.
- Реальные движения мыши не отправлялись. Удобство клика, средний клик и красную руну на живой камере необходимо оценить после перезапуска; превосходство над мышью не подтверждено.

# Две руки, красная руна и обзор рабочих столов — dev26

- 283 теста прошли. Новые сценарии: вход в Zoom без предварительного down, масштаб в обе стороны, отпускание Ctrl в каждом пакете, ошибка wheel с очисткой Ctrl, сохранение drag, обычный левый клик при второй открытой руке, отключение второй руки, потеря/скачок второй руки, перестановка результатов рук, Win+Tab/Win+D, выключение жеста рабочих столов и отсутствие реакции на обычное вертикальное наведение.
- Native smoke двух оверлеев Windows: синяя и красная SVG-руны, независимое положение красной, скрытие при паузе, завершение обоих потоков. Реальные движения мыши и сочетания в этом прогоне не отправлялись.
- Portable: `dist/20260919-151635/Gesture-Control-0.1.0.dev26-win64.zip`, 123.3 MiB, SHA256 `fa6743471f362b43d856d5d7c4f3deac11ec02f53ddf30e933e09ac6b45b4d08`. Frozen self-test (модель, inference, watchdog, GUI, трей) прошёл с PATH без Python. Обновлённая инструкция включена в архив.
- Вторая рука по умолчанию выключена. Основную руку следует активировать до появления второй. При полной потере основной руки восстановление по-прежнему требует одной открытой ладони. Красная руна — индикатор, не второй системный курсор. Ctrl+wheel поддерживается не всеми программами.
- Жесты с живой камерой, устойчивость при перекрещивании и ложные срабатывания четырёх пальцев ещё требуют пользовательской проверки. После включения второй руки при её присутствии одиночный pinch получает дополнительное окно арбитража 200 мс.

# Portable Windows x64 — 2026-09-19, dev25

- Финальный архив: `dist/20260919-144621/Gesture-Control-0.1.0.dev25-win64.zip`, 123.3 MiB. SHA256: `cbe4bdc8e061f0410044f79e8c261004668e4e96b4d5b85b178696e5f843e414`.
- 272 автоматических теста прошли. `pip check` не обнаружил конфликтов после установки сборочных зависимостей.
- Frozen self-test прошёл: SHA256 модели, профили и SVG, реальный MediaPipe inference на пустом кадре, OpenCV, отдельный spawn-watchdog с fake вводом, полный DesktopApp и нативный трей. Камера не включалась, системный ввод не отправлялся.
- ZIP распакован в другую папку с пробелами и кириллицей; 1513 файлов проверены по SHA256SUMS. EXE повторно прошёл self-test из другого рабочего каталога, с PATH только System32 и без PYTHONHOME/PYTHONPATH/VIRTUAL_ENV. Личные settings.user.json и camera-*.json не включены.
- Python физически не удалялся с тестового ПК. Проверка на чистой Windows/другом оборудовании, реальные жесты и камера в EXE остаются ручной приёмкой. Сборка не подписана.
- Воспроизводимость: `requirements-build.txt`, `GestureControl.spec`, `portable_entry.py`, `build-portable.ps1`, `tools/build_portable.py`. Повторная сборка создаёт новый каталог, не заменяет пользовательские файлы. Зависимости, ресурсы, модель, доступные лицензии и отчёт self-test включены в комплект.

# Системный трей — 2026-09-19, dev24

- 270 автоматических тестов прошли, включая 6 новых проверок очереди меню, сворачивания без остановки контроллера, восстановления окна, отказа скрытия при недоступном трее, маршрутизации команд и выхода.
- Native smoke на Windows: настоящий pystray и Tk, скрытие/возврат окна, эффекты OFF, открытие настроек и завершение потока трея. Контроллер подменный: камера и системный ввод при этой проверке не включались. Само отображение контекстного меню пользователем ещё требует ручной проверки.
- pystray 0.19.5 установлен, версии добавлены в requirements и pyproject. pip check не обнаружил конфликтов.
- Ручная проверка: запустить run-control.ps1, включить управление, свернуть окно крестиком, проверить управление и аварийную клавишу; из меню трея поставить паузу/продолжить, сменить эффекты, открыть настройки, выбрать «Выход» и проверить освобождение камеры. Перезапуск Explorer и восстановление значка после него отдельно не проверялись.

# Восстановление watchdog — 2026-09-19

- Предельное ожидание IPC увеличено с 80 до 200 мс; lease 400 мс не изменён. Добавлены сообщения input_response_slow, input_connection_lost и input_watchdog_reconnected.
- Реальные spawn-процессы с подменным вводом: задержка 120 мс не рвёт соединение; блокирующая отправка вызывает паузу, затем освобождает кнопку при EOF; Resume создаёт новый процесс только после завершения старого. Команда не переотправляется. Одна ладонь после ошибки ввода не восстанавливает управление.
- Подтверждены отказ замены при ненулевом exitcode и аварийный выход worker при невозможности отпустить принадлежащие ему клавиши. Прежние проверки heartbeat, смерти родителя, EOF и отпускания кнопок сохранены.
- Полный набор до дополнительной проверки очистки: 263 теста прошли; затем все 14 тестов input_guard, включая новую проверку неуспешного освобождения, прошли.
- Причина первоначальной системной задержки не установлена: исправлена реакция на краткие задержки и восстановление после обрыва. Живой SendInput не использовался. Поскольку IPC синхронный и interaction сериализует ввод, при зависшем ответе аварийный обработчик может ждать блокировку до 200 мс плюс очистка; критерий ТЗ 100 мс под такой нагрузкой этим изменением не подтверждается. Процесс, зависший внутри системного вызова, не заменяется принудительно.

# Разворот кисти в глубину — 2026-09-19

259 тестов прошли. Добавлен второй канал распознавания по x/z основания указательного пальца и мизинца, общий запрет повторного переключения для обоих каналов. Проверены повороты ±45° за 500 мс, зеркальность, отсутствие повторного переключения при немедленном обратном ходе, отбрасывание медленного поворота и дрожания глубины ±4°, полная отправка и освобождение Alt/Tab через подменный backend. Порог бокового движения снижен с 1.2 до 0.65; пороги разворота вынесены в JSON. Реальная камера и системное переключение окон в этом прогоне не использовались. Прежний таймаут watchdog этим изменением не исправлялся.

# Смахивание кистью — 2026-09-19

Заменён сигнал распознавания: направление от запястья к среднему положению PIP-суставов вместо координат центра ладони. Перенос и равномерное масштабирование руки не меняют сигнал. Добавлены проверки поворота вокруг неподвижного запястья, медленного поворота, быстрого переноса без поворота, согнутых кончиков пальцев. Срабатывания пишутся в INFO как wrist_flick_confirmed. Реальные жесты на камере ещё не проверены; прежний таймаут watchdog не изменён.

# Свайп открытой ладонью — 2026-09-19

Переключение окон перенесено с трёх пальцев на открытую ладонь, включая допустимое естественное сгибание. Порог скорости 1.2 ширины кадра/с, расстояние 0.14 ширины. Событие возникает во время движения; блокировка повторного свайпа не блокирует курсор. Сохранены прежние ключи привязок THREE_SWIPE_LEFT/RIGHT для совместимости профилей.

252 теста прошли: направления, отсутствие переключений при медленном наведении и захвате, отсутствие реакции на три пальца, срабатывание до остановки и продолжение наведения после свайпа. Проверки ввода используют подменный backend; реальные ощущения и частота случайных переключений требуют проверки пользователем. Таймаут watchdog этим изменением не исправлялся.

# Переключение окон — 2026-09-19, dev23

- Полный набор: 250 тестов прошли (`.venv\Scripts\python.exe -m unittest discover -s tests`).
- Новые сценарии: быстрый горизонтальный свайп, отсечение медленного/короткого/вертикального/диагонального движения, повтор после остановки без разрыва кадров, сброс при пропуске кадров, зеркальные направления, отключение жеста, приоритет мыши, подавление вызова клавиатуры после свайпа.
- Проверены порядок Alt/Shift/Tab и освобождение модификаторов при частичной ошибке отправки. Native SendInput в тесте заменён заглушкой; при ошибке отпускания одной клавиши предпринимаются попытки отпустить остальные.
- Реальное переключение окон и удобство порогов на живой руке ещё требуют ручной проверки. Поднимите указательный, средний и безымянный пальцы, сделайте быстрый горизонтальный взмах; остановитесь перед следующим. Стационарный жест сохраняет вызов клавиатуры.

# Проверка итерации 2 — 2026-09-17

Окружение: Windows x64, CPython 3.14.2, MediaPipe 1.0.1, OpenCV contrib 5.0.0.93. Полный набор версий: `requirements-cv-win-py314.lock.txt`.

- 20 автоматических тестов прошли, без пропусков в `.venv`. Включены реальные inference на сгенерированных пустых RGB-кадрах, отрисовка preview, остановка потоков при потере камеры/ошибке inference и предыдущие проверки input state machine.
- `pip check`: конфликтов зависимостей нет.
- Камера 0, backend auto, фактические кадры 640×480. Headless-запуск обработал 60 кадров и корректно завершился. В конце короткого прогона capture/tracking около 27–29 FPS, отдельные значения inference 9,8–18,6 мс. Это краткий smoke test, а не статистическая приёмка производительности.
- Оконный preview обработал 30 кадров и автоматически завершился без ошибки. Камера освобождена; последующий запуск смог открыть её повторно. Ручная проверка M/P/Q и закрытия крестиком ещё требуется.
- Запуск камеры занимал примерно 14–15 секунд. В конфигурации предусмотрен стартовый таймаут 30 секунд и отдельный таймаут 5 секунд после начала потока.
- В аппаратных прогонах модель вернула 0 рук. Точность определения одной/двух рук, анатомических Left/Right, стабильность при разном свете и пересечении рук ещё не подтверждены.
- Видео, кадры и landmarks аппаратной проверки на диск не записывались. Выполнялась только локальная обработка.
- MediaPipe выводит native-предупреждения feedback tensors/NORM_RECT, но inference завершается успешно. Полная задержка camera→cursor не измерялась; Windows input в этом этапе отключён.

Ручной сценарий перед следующей итерацией: запустить preview, показать каждую руку и затем обе, проверить 21 точку и метки сторон, убрать руки, проверить M/P и выход Q/Esc, повторный запуск после закрытия. Реальное управление и устойчивые ID рук пока не реализованы.

# Проверка итерации 3 — 2026-09-17

Пользователь подтвердил, что preview видит и распознаёт руки. Это подтверждает работоспособность предыдущего этапа на его камере, но не заменяет количественную приёмку ТЗ.

- 39 тестов прошли без пропусков. Новые сценарии: подавление синтетического шума, адаптация фильтра при быстром движении, dead zone и медленное накопленное смещение, достижимость четырёх углов, выбор стороны, перестановка рук в результате, появление второй руки, краткая/длительная потеря, неоднозначное пересечение, смена метки стороны, просроченные и некорректные timestamps/landmarks.
- Реальная модель по-прежнему проверяется на пустых сгенерированных кадрах. Интеграционная проверка рендера включает новую панель 1040×480.
- Визуально проверена отрисовка рабочей области и панели на синтетическом кадре. Это не снимок камеры и не измерение качества распознавания.
- Windows input по-прежнему отключён. Биометрическая идентификация и устойчивые tracks всех рук не заявляются: владелец виртуального указателя сопровождается по геометрии и стороне, при неоднозначности требуется R.
- Требуется ручная проверка: движение зелёного круга против серого креста, полный проход по углам рамки, появление второй руки, потеря основной и R, H, [ / ], независимость зеркальности M от движения виртуального указателя. Эргономика и camera→cursor latency ещё не приняты.

# Изменение области и выбор камеры — 2026-09-17

- Переназначены основные клавиши размера на −/+ (также =); добавлены кликабельные Area −/Area + и индикация ширины/высоты в процентах. Старые скобки оставлены как дополнительные клавиши: в русской раскладке они могут давать другие символы.
- Добавлен выбор камеры кнопками 0/1/2, цифрами 0–9, переключение C между 0/1, повторный запуск Retry. Номера устройств не подписываются как встроенная/внешняя без подтверждения: порядок зависит от Windows/backend.
- 47 тестов прошли. Новые проверки: совпадение мышиных областей с кнопками, новые клавиши изменения размера, выбор номера камеры, освобождение старого runtime до запуска нового, отбрасывание старых кадров, ошибка/таймаут с повторным запуском, быстрая смена выбора и отсутствие перезапуска уже выбранной рабочей камеры.
- Визуально проверена панель 1040×554 на синтетическом кадре: размер области, активная камера и шесть кнопок отображаются без обрезания.
- Аппаратная проверка смены 0→1 с backend auto не подтвердила получение кадров: камера 0 вернула ошибку MSMF -1072875772, камера 1 достигла таймаута ожидания и затем остановки драйвера. Причина (занятость другим preview, доступ или драйвер) не установлена. Ошибки не считать успешной аппаратной приёмкой. Перед ручным тестом закрыть прежнее preview; при проблемах проверить `--camera-backend dshow`.
- При ошибке интерактивный preview сохраняет выбор камеры/Retry. Headless завершает работу с ошибкой. Если старый драйвер ещё не завершил захват, новый не запускается одновременно; окно остаётся доступным для закрытия.
- Выбор камеры и размер сохраняются только в текущем сеансе; постоянные значения задаются `camera_index` и `work_area_*` в JSON.

# Активация, профили и клавиатура — 2026-09-17

Пользователь подтвердил, что камеры переключаются нормально. Обнаружен и прочитан новый документ `Gesture_Language_Windows_macOS_Android_v1.0.md`; согласование с основным ТЗ и расширение профилей зафиксированы в `Gesture_Profiles_Integration_v0.1.md`.

- 65 автоматических тестов прошли без пропусков. Новые проверки: классификация четырёх синтетических поз, hold/rearm без повторов, сброс выдержки при движении, запрет активации появлением руки, отсутствие движения виртуального указателя вне ACTIVE/POINT, callback остановки из отдельного потока, запрет жестового снятия PAUSED, потеря руки до/после активации и восстановление Resume, смена камеры/профиля, одноразовый запрос OSK и его отмена остановкой, неизменяемые системные назначения профилей.
- В Windows реально зарегистрирована `Ctrl+Alt+G`, в поток отправлено тестовое WM_HOTKEY, callback вызван, регистрация корректно освобождена. В этом прогоне dispatch callback около 0,03 мс; это проверка сообщения, не аппаратный замер от физического нажатия клавиш.
- Вызов OSK проверен с подменой WinAPI: выбирается системный osk.exe без текстовых аргументов, ошибка ShellExecute отображается как ошибка запуска. Реальное окно OSK и трёхпальцевый жест на камере требуют пользовательской проверки; текст не вводился.
- Визуально проверена панель на синтетическом кадре: состояние, Hold %, Profile, Pause, Resume и Keyboard отображаются. Снимок не содержит изображения пользователя.
- Профили Desktop/3D template/Video template загружаются и переключаются. Действия 3D/Video пока семантические: реальные адаптеры, сочетания и автоматический foreground switching не реализованы. Перечень программ запрошен у пользователя.
- Реальные cursor/click/key input и watchdog остаются следующим этапом. Процент успешности жестов и комфорт активации на реальных руках ещё не приняты.

## 2026-09-17 — системный курсор, dev6

- Добавлен явный режим `--control` и `run-control.ps1`; `--preview` остаётся виртуальным. Профили конкретных приложений отложены, существующие интерфейсы сохранены.
- Все 90 автоматических тестов прошли в `.venv`. Проверены края и физические координаты дисплеев 1920×1080, 3840×2160 и 1080×1920, ABI Windows INPUT и флаги SendInput, плавное начало и ограничение скорости после задержки, привязка к текущей позиции после смены позы/Resume, отсутствие команд до активации, после остановки, при потере руки/горячей клавиши и на устаревших кадрах. Ошибка отправки и изменение размера экрана проверены с подменённым Windows API. Общие сценарии взаимодействия повторены с подставным cursor backend.
- Проверены CLI `--help` и fake `--demo`, завершение с кодом 0. Проверки MediaPipe на синтетических кадрах и прежнего preview входят в общий набор.
- Нативный Windows backend загружается; при пробном чтении через `GetCursorPos` Windows отказала в доступе к позиции курсора в среде выполнения агента. Реальный SendInput не выполнялся. Движение системного курсора, комфорт скорости и масштабирование DPI на рабочем столе пользователя требуют ручной проверки; автоматические тесты не подменяют её.
- Курсор работает на основном мониторе; при начале наведения физическая мышь должна находиться на нём. Перемещения синхронные, без очереди и удерживаемых кнопок. Pinch/click/drag, scroll, клавиатурный ввод и отдельный watchdog ещё не подключены; это не завершение всей итерации владения вводом из ТЗ.

Ручная проверка: запустить `run-control.ps1`, активировать ладонью, перейти к POINT, проверить плавное начало и края рабочей области. Проверить Ctrl+Alt+G из другого окна, потерю руки, Resume с повторной активацией, смену камеры и Area. До следующего этапа клики выполняются обычной мышью.

## 2026-09-17 — восстановление рукой, левый pinch/drag и watchdog, dev7

Пользователь подтвердил работу системного курсора и сообщил, что после потери руки ладонь не возвращает управление. Причины: общая ручная PAUSED для потери tracking и аварийной остановки, а также заблокированный выбор владельца VirtualPointer. Для потери tracking добавлено отдельное разрешение повторного выбора одной руки и активации после полной выдержки открытой ладони; остальные причины паузы требуют ручного Resume. Соответствующее уточнение внесено в §67 ТЗ.

- Все 107 тестов прошли. Добавлены проверки гистерезиса pinch, подтверждения down/up, обязательного начального размыкания, отсутствия повторов при удержании, фиксации курсора до порога drag, движения после порога, немедленного up при пропуске руки и запрета возобновления старого drag.
- Проверены восстановление только после подтверждённой одиночной ладони, отказ при двух руках и сохранение ручной аварийной паузы. Смена камеры/профиля, аварийная остановка и закрытие освобождают принадлежащий ввод. Проверена очистка процесса при ошибке hotkey в конфигурации.
- Watchdog исполняет input в отдельном spawn-процессе. На fake backend реально проверены истечение heartbeat без основного цикла, EOF канала и принудительное завершение отдельного родительского тестового процесса: получено освобождение принадлежащей кнопки. После таймаута pulse не снимает блокировку, нужна новая сессия. Ошибка освобождения сохраняет владение для повторной попытки.
- Нативные флаги down/up и отказ захвата уже нажатой физической кнопки проверены с подменой WinAPI. В Windows реально выполнены запуск watchdog, arm, heartbeat, stop и завершение, без отправки мышиного ввода. Реальные клики и drag в приложениях пользователя агент не выполнял.
- Порог начала drag пока нормализованный, а не DIP. Правый клик, отдельная стабилизация двойного клика, прокрутка, уведомления lock/suspend и полный keyboard context остаются работой следующих этапов. Испытания на 3 пользователях и аппаратные критерии времени из ТЗ не проведены. Ограничения watchdog при отказе ОС/одновременном завершении процессов сохранены в README.

Ручная проверка: запустить `run-control.ps1` с Desktop, удержать ладонь, навести указатель на безопасный элемент, соединить большой/указательный пальцы и разомкнуть — один левый клик. Для drag удержать соединение и переместить ладонь; разомкнуть для отпускания. Проверить потерю руки во время drag, повторную активацию одной ладонью, затем Ctrl+Alt+G и невозможность снять аварийную паузу ладонью без Resume. Настройки pinch и порога drag находятся в `settings.example.json`.

## 2026-09-17 — наведение ладонью и устойчивое восстановление, dev8

Пользователь сообщил о редких успешных click/drag, рывках и невозможности восстановить управление после потери руки; попросил наведение открытой ладонью. Реальный проблемный видеопоток/лог не предоставлен, поэтому не заявляется точное воспроизведение всех аппаратных симптомов.

- По умолчанию PALM: курсор следует сглаженному центру запястья/MCP, движение пальцев не меняет точку наведения. Открытая ладонь включает управление и затем наводит; повторное удержание в ACTIVE не выключает его. INDEX сохраняет прежний способ как настройку. Маркер preview перенесён на центр ладони.
- Для активации PALM и восстановления после tracking loss использована выдержка внутри ограниченной пространственной области вместо мгновенной скорости между шумными landmarks. Короткие ошибки позы/детекции до 180 мс не обнуляют весь прогресс, но не засчитываются в выдержку. Длительная потеря, быстрое перемещение и аварийная пауза не активируют управление.
- Найден путь дополнительной блокировки: истечение команды перемещения между родителем и процессом ввода считалось ошибкой всей сессии, после которой жестовое восстановление запрещено. Теперь такой move возвращает False и отбрасывается, а кнопки, другие сессии и реальные ошибки ввода сохраняют строгую обработку. Watchdog не отключён.
- Сглажены координаты ладони во время drag. Одиночная ошибка классификации при обычном наведении замораживает движение без повторного чтения позиции и сброса привязки.
- Все 116 тестов прошли. Новые сценарии: длительное наведение открытой ладонью без выключения; пять циклов потери/возврата с шумом и ошибками позы; короткие пропуски детекции; аварийная пауза с последующей потерей руки остаётся ручной; движение кончиков пальцев не сдвигает palm pointer; энергия синтетического дрожания курсора менее 20% исходной; просроченный move не переводит lease в fault. Регрессии INDEX и прежние проверки pinch/watchdog сохранены.

Ручная проверка dev8: перезапустить `run-control.ps1`, удержать открытую ладонь около 0,8 с, затем вести всей ладонью без сгибания пальцев. Проверить неподвижный курсор, наведение, pinch/drag, несколько исчезновений руки и восстановление ладонью. При иной ошибке смотреть строку состояния/сообщение консоли: ручная аварийная пауза, сбой backend и tracking recovery имеют разные условия продолжения. Качество нового режима на реальной камере пользователя пока не подтверждено.

## 2026-09-17 — указательный палец и потеря tracking одновременно с watchdog, dev9

- По уточнению пользователя поза управления остаётся открытой ладонью, но точка курсора возвращена на кончик указательного. Настройки разделены: pointer_mode=PALM, pointer_anchor=INDEX; центр ладони сохранён как отдельная настройка. Маркер камеры и подпись Index tip согласованы с координатой курсора.
- Воспроизведён конкретный дефект: после задержки дольше обоих таймаутов InteractionSession сначала отправляла heartbeat уже остановленному watchdog и превращала потерю руки в ручную input-error паузу. Регрессионный тест до исправления падал с `Input watchdog stopped the session. Click Resume` и recover_tracking=False.
- Теперь сначала обрабатывается потеря tracking и закрывается прежняя сессия через stop, который допускается после истечения lease. Новая сессия запускается только после подтверждённой ладони. Ошибки IPC и ручная аварийная остановка не объявляются потерей руки и не обходятся.
- Все 120 тестов прошли. В том числе реально запущен отдельный процесс watchdog с fake desktop backend: активация, остановка основного цикла на 500 мс, истечение lease, потеря tracking, повторная выдержка ладони и успешное выполнение anchor в новой сессии. Затем проверен запрет жестового снятия ручной аварийной паузы. Реальные Windows mouse-команды в этих тестах не отправлялись.
- Отдельно проверены выход кончика указательного за рабочий прямоугольник (clamp к краю, ACTIVE сохраняется), возврат и дальнейшая отправка перемещений. Повторно прошёл тест MediaPipe/отрисовки после обновления подписи preview.

Ручная проверка: перезапустить run-control.ps1; убедиться, что вверху написано Index tip. Сравнить выход только за прямоугольник и полное исчезновение руки из кадра. После полного исчезновения — показать открытую ладонь до подтверждения. Точный сценарий пользователя без его лога/видеопотока не подтверждён; исправлен воспроизведённый конфликт tracking/lease, и сохранена диагностика иных ошибок в строке состояния/консоли.

## 2026-09-17 — точность pinch и область 100%, dev10

Полный набор: 129 тестов прошли в `.venv`, включая прежние проверки восстановления и watchdog.

Пользователь подтвердил восстановление управления. Следующее замечание — уход курсора при щипке, особенно на маленьких кнопках окна; также запрошена область 100%.

- Добавлена отдельная стадия PinchAim: раннее сближение фиксирует наведение до подтверждения down, не создавая клик. Отмена, таймаут и tracking loss снимают фиксацию. После up наведение стабилизируется на 120 мс. Подтверждение клика и порог drag сохранены.
- В example-конфиге установлен полный кадр. Добавлены Area 100% / B; расширение смещённой области теперь может достигать полного кадра, корректируя её центр у границ. Во время control смена области сохраняет паузу и освобождение ввода.
- Новые проверки: медленное и быстрое сближение, неподвижная точка от AIM_LOCK до down/up, отсутствие клика при отмене и потере руки, таймаут незавершённого щипка, сохранение длительного удержания для drag, кнопка/клавиша полного кадра и возврат с 100% к 95%. В синтетическом сценарии плавного сближения до фиксации смещение менее 4 px по каждой оси на 1920×1080; после фиксации перемещений нет. Это не аппаратный замер на руке пользователя.

Ручная проверка: перезапустить run-control.ps1, проверить Area 100% × 100%, навести указательный на небольшую кнопку и сделать щипок; при начале сближения появляется AIM_LOCK. Затем проверить отмену щипка, перетаскивание и потерю руки. Реальные попадания по кнопкам Windows агент не выполнял.

## 2026-09-17 — правый клик и арбитраж pinch, dev11

- Выполнен следующий этап §68.2: большой + средний дают правый down/up; настройка RING заменяет средний на безымянный. В Desktop добавлено семантическое назначение PINCH_RIGHT → CONTEXT_MENU; шаблоны приложений не исполняют этот жест.
- Два одновременно сомкнутых новых pinch отклоняются до размыкания; текущий левый/правый захват имеет приоритет. Правый pinch фиксирует курсор и не начинает drag. Освобождение при tracking loss использует фактически принадлежащую кнопку, а не жёстко заданную левую.
- Все 141 тест прошли. Новые проверки: одна правая пара down/up без повторов при удержании, отсутствие движения при смещении руки, конфликт двух pinch и повторное размыкание, сохранение левого drag/правого захвата, альтернативный палец, потеря руки и аварийная остановка, отсутствие исполнения в 3D-шаблоне. Нативные right down/up, маска освобождения двух принадлежащих кнопок и отказ захвата физически нажатой правой кнопки проверены с подменой WinAPI.
- Отдельный реально запущенный watchdog с fake backend освобождает правую кнопку после истечения heartbeat. Реальные контекстные меню на рабочем столе пользователя не открывались; новый жест требует ручной проверки. Прежние проверки левого клика, AIM_LOCK, площади и восстановления также прошли.

Проверка пользователем: запустить run-control.ps1, активировать ладонью, навести указатель на безопасный элемент и соединить большой/средний пальцы, затем разомкнуть. Проверить один вызов контекстного меню, отсутствие перетаскивания и возможность вернуться к левому клику после размыкания. Следующие этапы: §68.3 (двойной клик) и §69 (scroll); завершение всего ТЗ не заявляется.

## 2026-09-18 — возврат после правого клика и непрерывность наведения, dev12

Полный набор: 146 тестов прошли, включая прежние проверки кликов, drag, площади и watchdog.

Пользователь подтвердил правый клик, но сообщил о зависании после него и рывках. Строка состояния недоступна: окно уже закрыто. Поэтому точный аппаратный сценарий не считается подтверждённым.

- Три новых теста до исправления падали: AIM_LOCK повторно включался на неподвижном полуразомкнутом щипке; чередующийся шум расстояния принимался за начало щипка; после правого up со слегка согнутым средним пальцем команды движения не появлялись.
- После up и во время чужого захвата повторный pre-aim заблокирован до полного раскрытия. Настоящий следующий down по гистерезису PinchGate остаётся доступен; поддержка повторного клика не удалена. Начало pre-aim требует направленного сближения, статическое расстояние само по себе не замораживает курсор.
- RELAXED_PALM разрешает наведение с согнутым средним/безымянным в режиме PALM. Она не является жестом активации и не заменяет THREE_FINGERS для OSK.
- Уменьшена dead zone до 0.001. CursorMotion плавно приближается к цели с временной константой 60 мс и прежним пределом скорости. Проверены монотонное приближение без перелёта, точное установление в цели и запрет активации расслабленной позой. Добавочное сглаживание увеличивает задержку отклика; качество на реальной камере требует ручной оценки.

Ручная проверка: перезапустить run-control.ps1, выполнить правый клик и разомкнуть большой/средний, не обязательно полностью выпрямляя средний. Проверить движение по меню, возвращение к обычному наведению, повторный левый/правый клик, drag и восстановление после потери руки. Точка курсора остаётся на указательном, рабочая область в example-конфиге — 100%.


# dev13 — независимый цикл управления и запуск камеры — 2026-09-18

Пользователь сообщил, что dev12 не устранил подвисания. Обнаружена архитектурная зависимость обработки жестов и heartbeat от оконного event pump. Обработка вынесена в ControlLoop; UI-вызовы imshow/waitKeyEx/OSK выполняются вне его блокировки. Это подтверждённое устранение зависимости, а не доказательство причины каждого аппаратного зависания.

Добавлены шесть проверок: окно задерживается на 600 мс, а реальный run_preview продолжает обработку; lease не истекает при задержке UI; ошибка управляющего потока освобождает кнопку; модель создаётся параллельно захвату; ошибка создания модели передаётся через runtime; headless max_frames завершает потоки. Native UI в интеграционном тесте заменён заглушками, системный ввод — фальшивый.

Старт модели перенесён в inference-поток. Добавлены отдельные замеры camera_opened/open_ms/configure_ms, camera_first_frame/elapsed_ms, tracker_ready/elapsed_ms. Отключено только автоматическое MSMF HW-transform probing, пользовательская переменная окружения имеет приоритет. Предыдущий аппаратный замер 14–15 секунд не разделял этапы. Новый аппаратный замер скорости запуска и ручная проверка правого клика не выполнялись; ускорение не заявляется как измеренное.

Режим --control сохраняет ротационный журнал logs/gesture-control.log, без видео/landmarks. Все жесты, профили и область управления сохранены.

Итог: .venv\Scripts\python.exe -m unittest discover -s tests — 152 теста, OK (7,17 с).


# dev14 — близкая камера и неоновый курсор — 2026-09-18

- В example-конфиге область 50% × 50%, FX FULL. Near 50% / N сбрасывает область в центральную половину; B сохраняет возможность 100%. Изменение во время drag сначала освобождает ввод и ставит PAUSED.
- Добавлен CursorOverlay: отдельный Tk-поток, полый синий круг, 12 вращающихся геометрических рун; FULL/MINIMAL/OFF по V или кнопке. Положение читается из Windows в физических координатах; ввода эффект не генерирует. При PAUSED/OFF/устаревшем состоянии эффект скрыт.
- 157 автоматических тестов прошли (7,31 с). Новые проверки покрывают достижимость всех углов из центральной половины при обоих режимах зеркала, освобождение drag при изменении области и геометрию эффекта.
- Native smoke: окно эффекта реально создано, показано и закрыто в Windows. Синтетическая read-only позиция (300,300) позволила проверить ветку отрисовки без перемещения системного курсора. IsWindowVisible=True, extended styles содержат layered/transparent/toolwindow/noactivate, WindowFromPoint не выбирает окно эффекта, foreground window не изменилось, поток завершён без ошибки. Реальные клики не генерировались.
- Ручная оценка внешнего вида поверх пользовательских приложений, разных DPI и удобства области с реальной рукой остаётся за аппаратной проверкой. Прозрачность/окно проверены API, не визуальным скриншотом.


# dev15 — ложная фиксация и точность — 2026-09-18

Исследован существующий logs/gesture-control.log: при capture/tracking около 30 FPS зафиксированы ACTIVE/OPEN_PALM/AIM_LOCK, краткие UNKNOWN и отдельные реальные потери руки с hands=0. Журнал не содержит landmarks и не доказывает ложность каждого отдельного щипка. Код позволял начинать левый AIM_LOCK при уменьшении расстояния независимо от абсолютной близости пальцев.

Для OPEN_PALM ранняя фиксация теперь требует попадания в pinch_aim_ratio; для POINT сохранено раннее обнаружение. Добавлен ограниченный 100 мс допуск UNKNOWN только на свежих валидных кадрах выбранной руки после навигации. Cancel обнуляет историю этого допуска.

Настройки OneEuro: min_cutoff 1.0, beta 4.0, остальные параметры прежние. В синтетическом тесте 30 Гц, стационарный сигнал 0.5 ± 0.003, затем скачок до 0.7: размах установившегося шума фильтра на шкале 1920 px снизился с 1.57 до 1.16 px; достижение 90% скачка ускорилось с 267 до 133 мс. Это результат только фильтра на заданном сигнале, не измерение полной задержки camera→cursor и не аппаратная оценка точности.

160 тестов прошли (7.31 с). Новые регрессии проверяют отсутствие дальнего AIM_LOCK на открытой ладони, ограниченный допуск UNKNOWN без бесконечного продления и сравнительное поведение фильтра. Существующие проверки кликов, точного раннего наведения, потери руки и watchdog проходят. Ручная проверка после резких движений на пользовательской камере ещё требуется.


# dev16 — двойной pinch и остановившееся сближение пальцев — 2026-09-18

Реализована первая часть §68.3: две естественные пары левых down/up с ограниченной стабилизацией точки. Пара проверяется по времени двух нажатий и половинам ширины/высоты системного прямоугольника. Системные параметры читаются через input watchdog, первый клик не задерживается, дополнительных событий doubleClick нет. Drag, правый клик, отмена и потеря руки сбрасывают кандидата. Отдельное назначение DOUBLE_CLICK на альтернативный жест не реализовано в этой итерации.

Пользователь дополнительно описал фриз при расслабленных близких пальцах. В PinchAim добавлен настраиваемый лимит отсутствия прогресса 150 мс (уменьшение отношения расстояния минимум на .02 обновляет прогресс). Удерживаемая кнопка не отменяется этим лимитом; это отмена только неподтверждённого AIM_LOCK. Причина конкретного пользовательского эпизода без записи состояния не доказана.

169 автоматических тестов прошли (8.90 с): новая интеграция двух pinch проверяет ровно четыре события, сохранение точки, отсутствие второй команды при потере руки и слишком короткий системный интервал. Проверены границы прямоугольника, время от down, drag/reset, выход по движению, прекращение фиксации расслабленных пальцев. Существующие сценарии click/drag/правый click проходят. Native read-only вызов системных параметров выполнен отдельно; реальный double-click в Проводнике не выполнялся.


# dev17 — предоставленная анимированная SVG-руна — 2026-09-18

Неизменённый SVG скопирован в package assets; равенство байтов подтверждено. Добавлен специализированный Canvas-адаптер M/L/H/V/A/Z, кругов, пунктирных линий и групп данного SVG. 54 формы представлены 96 Canvas-объектами; 120 обновлений не увеличили число объектов, среднее время вызова обновления в локальном Tk smoke составило 1.26 мс. Это не измерение полной задержки курсора.

172 теста прошли (7.71 с), включая геометрию относительных команд/дуги, направления вращения, доставку короткого события клика и отсутствие создания новых объектов при анимации. Native overlay smoke с подставленной read-only позицией: окно видимо, пропускает hit-test, фокус не изменён, FULL/MINIMAL/OFF и скрытие при PAUSED работают, поток завершён без ошибки. Системный ввод не генерировался. Gaussian blur стилизован контурами, фоновый градиент не рисуется; пиксельная идентичность браузерному SVG не заявляется.


# dev18 — clean HUD и устранение чёрной подложки

Новый SVG сохранён без изменения байтов. Адаптер поддерживает viewBox 640, новые имена классов, CSS var цветов, отдельные subpath после M. Фоновый круг opacity .035 исключён; цветовой fade к чёрному и непрозрачные тёмные glow-контуры удалены в FULL/MINIMAL. Цвет Canvas/окна заменён на явный прозрачный ключ magenta; SetLayeredWindowAttributes проверяется на ошибку.

174 теста прошли (7.61 с). Дополнительные проверки подтверждают только светлые цвета, отсутствие фоновой фигуры, правильный масштаб и четыре раздельные линии перекрестия. Native smoke создал анимированный overlay на синтетической позиции без ввода: GetLayeredWindowAttributes подтвердил COLORKEY=0xff00ff и флаг LWA_COLORKEY; фокус не изменён, ошибок анимации и завершения нет. Визуальный скриншот поверх пользовательского приложения не снимался. Полупрозрачное браузерное размытие заменено чистыми контурами, не заявляется пиксельная идентичность SVG.


# dev19 — вертикальная прокрутка двумя пальцами

Реализован подтверждённый TWO_FINGERS, отдельный ScrollGesture, арбитраж с захватом и native MOUSEEVENTF_WHEEL через существующий watchdog. Используется смещение выбранной ладони, нормированное на рабочую область, с dead zone, пределом скорости и дробным остатком. Неподвижная рука не создаёт повторов, превышение лимита не накапливается. Native delta кодируется signed в DWORD, проверяется срок команды.

184 теста прошли (7.66 с). Новые тесты: классификация, 10 секунд покоя, движения в обе стороны, инверсия, ограничение скорости без очереди, дробный остаток, повтор кадра, 20 входов/выходов без wheel/click/move, эксклюзивность scroll, выход в pinch без случайного клика, потеря руки, приоритет held pinch, валидация настроек/команд. SendInput проверен заглушкой: флаг 0x0800 и знаки ±120, просроченный ввод отклоняется. Реальное колесо в пользовательских приложениях не отправлялось; аппаратная проверка удобства и распознавания позы нужна.

Контракт wheel: https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-mouseinput . Кулак/Esc, swipe, zoom и дальнейшие пункты §69 остаются отдельными следующими итерациями.


# dev20 — ожидание руны и фиксация при сгибании указательного пальца

187 тестов прошли (8.48 с). Проверены: 2-секундный срок видимости после tracking-loss без бесконечного продления, возвращение ACTIVE, немедленная ручная пауза/OFF, сокрытие при устаревших обновлениях. Изменение только презентационное, grace владения вводом не увеличен.

Новая интеграционная регрессия воспроизводит сгибание указательного пальца вниз на открытой ладони с одновременным уменьшением pinch-расстояния. Курсор остаётся в исходной точке во всех шести кадрах подхода, кнопки не нажимаются. Прежняя регрессия движения только большого пальца не включает AIM_LOCK. Существующие click/double-click/drag/scroll проверки проходят. Устранение пользовательского смещения на реальной камере ещё не подтверждено.
# Кулак → Esc — 2026-09-18

Полный прогон `python -m unittest discover -s tests`: **221 тест прошёл**, включая 6 новых сценариев FIST/Esc.

Реализован дискретный FIST/CANCEL по §69: геометрия согнутых DIP/TIP, временное подтверждение 500 мс, один down/up, rearm через навигационную позу, блокировка во время захвата и скролла. Переключатель в библиотеке жестов, параметры в Settings и example. Backend Windows поддерживает только необходимый сейчас ESC; другие клавиши ещё не объявлены поддержанными. Описание KEYBDINPUT/KEYEVENTF_KEYUP сверено с [Microsoft](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-keybdinput).

Тесты проверяют отличие от расслабленной ладони, ровно одну пару событий, повторное разжатие, отсутствие Esc в STANDBY/PAUSED и при потере руки, приоритет drag/scroll, освобождение клавиши при ошибке KEY_UP. Нативная упаковка INPUT проверена через подставной SendInput; реальное нажатие в чужое окно не отправлялось. Проверка реального кулака на пользовательской камере и действия в целевых приложениях остаётся ручной приёмкой.
# dev22 — привязка к центру ладони

До правок создан ZIP версии dev21: 107 файлов, CRC и SHA-256 всех файлов проверены после записи. Включены текущие пользовательские настройки и модель. После снимка включён PALM в значениях по умолчанию, example и пользовательском JSON; сохранён доступный через UI INDEX. Калибровка и отображение точки согласованы с выбранным якорем, чужой тип калибровки отклоняется (старые профили считаются INDEX).

222 автоматических теста прошли. Новый интеграционный сценарий: неподвижные основания ладони, сгибание пальцев, одна пара down/up — позиция курсора не меняется; legacy-тесты указательного пальца теперь явно задают INDEX. Это синтетическая проверка геометрии, не измерение точности реальной камеры.
# Скролл после перехода на ладонь

225 тестов прошли: добавлены возврат к наведению при UNKNOWN-позе после отпускания скролла на свежих кадрах ладони; усиление повторных свайпов в зависимости от частоты; сброс при смене направления; сохранение серии при возврате к курсору и очистка при полном reset. Физическая скорость прокрутки в конкретных программах требует ручной проверки.
# Быстрые свайпы и восстановление открытой рукой

227 тестов прошли. Новый сценарий перемещает только кончики двух пальцев при неподвижной ладони: за три кадра 30 FPS начинается скролл без mouse-down. Второй сценарий после потери руки проверяет отсутствие активации при сомкнутых большом/указательном и восстановление за семь кадров после размыкания. Длительности подтверждения: старт 250 мс, восстановление 150 мс, быстрый свайп 50 мс. Проверки с синтетическими landmarks не подтверждают качество распознавания реальных быстрых движений камерой.
# Скролл и ложное выделение текста

232 теста прошли. Добавлены два пальца с касанием большого: только SCROLL без POINTER_DOWN; запрет повторного клика после скролла до раскрытия ладони; короткий возврат без обратного wheel и подтверждённая смена направления при длительном движении. Проверки rate cap/inversion отдельно используют отключённый фильтр реверса, поведение включённого фильтра проверяется новым сценарием. Реальное выделение текста в пользовательском окне не воспроизведено: исправлены найденные пути конфликта в арбитраже, аппаратная проверка остаётся необходимой.
# Два пальца → средняя кнопка мыши

238 тестов прошли, включая 6 новых: подтверждение down и единственный up, middle-drag от центра ладони, отпускание при потере руки без повторного захвата, ручная пауза, приоритет левого drag, нативные middle-down/up и проверка физически удерживаемой средней кнопки. Native SendInput заменён тестовым приёмником — реальные действия в пользовательских окнах не выполнялись. Старые тесты скролла явно выбирают SCROLL; новый дефолт MIDDLE не отправляет wheel. Проверка в браузере и 3D-приложении остаётся ручной.
