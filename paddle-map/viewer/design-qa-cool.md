# Прохладная версия: визуальная и функциональная проверка

Исходник: `../data/art/generated-atlas-cool.png`, 1254 × 1254, встроенный ImageGen.
Рендер: `../data/art/rendered-atlas-cool.png`, 2048 × 2048 CSS px.
Одинаковые географическая рамка, север сверху, zoom 14, без подписей/оболочки.
Парное сравнение: `../art-render-comparison-cool.png`, 1600 × 800,
каждая сторона нормализована до 800 × 800. Слева исходник, справа WebP-тайлы.

Крупных различий в кадре и палитре, пустот или заметных швов нет. Рендер мягче
исходника из-за уменьшения и WebP. Жёлтый оттенок уменьшен, кроны объёмнее,
вода темнее и спокойнее по фактуре. Точная копия пользовательского референса
и сохранность навигационной геометрии не заявляются.

`../preview-cool-desktop.png`: 1440 × 1000.
`../preview-cool-mobile.png`: 390 × 844.
Шрифты и оболочка сохранены, все три кнопки помещаются на узком экране.
Переключение тёплой/прохладной/векторной карты сохраняет положение камеры.
Подписи и диагностический слой сохраняют самостоятельность.

Исправленная P1-ошибка: при касании южной границы MapLibre запрашивал соседний
несуществующий ряд и пытался декодировать HTML как картинку. Границы растрового
источника сдвинуты внутрь на 1e-8 градуса; последующая проверка Азова, возврата,
переключений и узкого экрана дала 0 ошибок карты и отсутствие alert.

Все 25 тайлов прошли декодирование и проверку размеров. Стиль прошёл валидацию.
Production-сборка проходит; предупреждение Vite о размере веб-бандла осталось.
P3: при сильном увеличении видна ограниченность исходного разрешения.

final result: passed
